# Production dependencies — pinned exactly so every environment (local, CI,
# Render) runs identical code. Change versions deliberately, never by drift.
streamlit==1.39.0
pandas==2.2.0
plotly==5.24.0
python-dotenv==1.0.1
pydantic==2.14.0a1
google-genai==1.75.0
ddgs==9.14.4
requests==2.34.2
